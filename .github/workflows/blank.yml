# This file was generated by upptime/uptime-monitor@v1.30.1
#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the template updates (daily)
# Instead, change your .upptimerc.yml configuration: https://upptime.js.org/docs

name: Secret CI
on:
  workflow_dispatch:
jobs:
  release:
    name: test secret
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Use secret in script
        env:
          MY_SECRET1: ${{ secrets.NOTIFICATION_TELEGRAM }}
          MY_SECRET2: ${{ secrets.NOTIFICATION_TELEGRAM_BOT_KEY }}
          MY_SECRET3: ${{ secrets.NOTIFICATION_TELEGRAM_CHAT_ID }}
        run: |
          echo "My secret value: $MY_SECRET1"
          echo "My secret value: $MY_SECRET2"
          echo "My secret value: $MY_SECRET3"
          # Use $MY_SECRET in your script
